# 담임 행정 허브 설계

## 개요

이 문서는 담임교사 개인용 웹앱 **담임 행정 허브: 오늘 처리할 업무함**의 첫 버전 설계이다. 앱의 목적은 공문, 제출물, 반복 문구, 마감 일정을 한곳에 모아 선생님이 매일 처리해야 할 행정 업무를 놓치지 않게 돕는 것이다.

첫 버전은 **담임교사 개인용**으로 설계한다. 공동 담임, 학년부 공유, 관리자 배정, 학교 시스템 연동은 넣지 않는다. 사용자는 자신의 학급, 공문 업무, 제출물 수합, 문서 템플릿, 개인 마감을 직접 관리한다.

첨부 파일은 앱에 업로드하지 않는다. 대신 구글드라이브 링크, 학교 메신저 위치, 업무포털 문서번호, 로컬 폴더 위치 같은 **외부 링크와 위치 메모**만 저장한다.

첫 버전은 로그인 기반 개인 저장을 전제로 한다. 모든 데이터는 `userId` 아래에 저장한다. 로컬 브라우저 저장만 사용하는 임시 도구가 아니라, 같은 교사가 다시 접속했을 때 자신의 업무를 이어서 볼 수 있는 계정 기반 웹앱으로 설계한다.

## 목표

- 담임교사가 오늘 처리할 행정 업무를 한 화면에서 확인한다.
- 공문 마감, 제출 대상, 첨부 위치, 처리 상태를 체크리스트로 관리한다.
- 학생별 제출 여부를 빠르게 체크하고 미제출 현황을 바로 파악한다.
- 자주 쓰는 안내문, 상담 기록 양식, 보고 문구를 템플릿으로 저장하고 재사용한다.
- 학교 업무, 학급 업무, 개인 업무 마감을 색상별 캘린더로 확인한다.

## 대상 사용자

- 주 사용자: 담임교사 1명
- 보조 사용자: 없음

학생이나 학부모가 직접 접속하는 기능은 첫 버전에 포함하지 않는다. 제출물 수합판도 학생이 제출하는 시스템이 아니라, 교사가 받은 제출물을 직접 체크하는 도구이다.

## 첫 버전 범위

첫 버전에 포함할 기능은 다섯 가지이다.

1. **오늘 업무함**
   - 지난 마감, 오늘 마감, 미완료 제출물, 이번 주 예정 업무를 모아 보여준다.
   - 업무 항목을 완료 처리하거나 상세 화면으로 바로 이동할 수 있다.

2. **공문 처리 체크리스트**
   - 공문 제목, 마감일, 제출 대상, 담당자 메모, 첨부 링크/위치, 처리 상태를 관리한다.
   - 처리 상태는 `접수`, `처리 중`, `제출 대기`, `완료`, `보관`으로 나눈다.
   - 공문에서 제출물 수합이 필요하면 수합판을 연결할 수 있다.

3. **제출물 수합판**
   - 학생 명부를 바탕으로 동의서, 사진, 과제, 신청서 등의 제출 여부를 체크한다.
   - 학생 명부는 직접 입력과 CSV/표 붙여넣기를 모두 지원한다.
   - 제출 상태는 `미제출`, `제출`, `보완 필요`, `해당 없음`으로 관리한다.

4. **반복 문서 템플릿함**
   - 자주 쓰는 안내문, 상담 기록 양식, 보고 문구, 알림장 문구를 저장한다.
   - 템플릿은 실제 상담 기록 저장소가 아니라 반복 양식과 문구 저장소이다.
   - 템플릿에는 `{학급}`, `{마감일}`, `{준비물}`, `{제출물명}` 같은 간단한 치환값을 사용할 수 있다.

5. **마감 캘린더**
   - 학교 업무, 학급 업무, 개인 업무를 색상별로 표시한다.
   - 공문과 제출물 수합판의 마감일은 캘린더에 자동으로 나타난다.
   - 날짜를 선택하면 해당 날짜의 업무 목록을 볼 수 있다.

## 첫 버전 제외 범위

다음 기능은 첫 버전에서 제외한다.

- 파일 업로드와 앱 내부 파일 저장
- 학생이나 학부모의 직접 제출 화면
- 공동 담임, 학년부, 부서 공동 편집
- 학교 메신저, 업무포털, NEIS, K-에듀파인 연동
- 공문 파일 자동 분석
- 실제 상담 기록 누적 관리
- 보호자 연락 기록장
- 알림톡, 문자, 이메일 자동 발송
- 학교 관리자용 통계 화면

이 제외 범위는 앱을 담임 개인의 행정 정리 도구로 유지하고, 개인정보와 파일 저장 리스크를 줄이기 위한 결정이다.

## 화면 구조

### 오늘 업무함

첫 화면은 **오늘 업무함**이다. 화면은 업무 카드 네 섹션과 빠른 추가 액션으로 구성한다.

| 영역 | 표시 조건 | 비고 |
| --- | --- | --- |
| 지난 마감 | 마감일이 지났지만 완료되지 않은 항목 | 가장 위에 표시 |
| 오늘 마감 | 오늘까지 처리해야 하는 공문, 제출물, 개인 업무 | 수합판은 미완료율 함께 표시 |
| 미완료 제출물 | 마감일이 7일 안이고 제출 완료율이 100%가 아닌 수합판 | 지난 마감/오늘 마감에 해당하지 않을 때 표시 |
| 이번 주 예정 | 내일부터 7일 안에 마감되는 미완료 항목 | 위 세 영역에 해당하지 않을 때 표시 |

빠른 추가 액션은 공문, 제출물 수합판, 개인 마감, 템플릿 추가 버튼으로 제공하며, 업무 카드 섹션과 별도로 고정한다.

업무 항목에는 제목, 유형, 마감일, 상태, 연결된 제출물 수합판 여부, 첨부 위치 여부가 보인다. 사용자는 목록에서 바로 완료 처리하거나 상세 화면으로 들어간다.

오늘 업무함은 같은 항목을 여러 영역에 중복 표시하지 않는다. 표시 우선순위는 `지난 마감`, `오늘 마감`, `미완료 제출물`, `이번 주 예정` 순서이다. 예를 들어 오늘 마감이면서 미완료 제출물이기도 한 수합판은 `오늘 마감`에만 표시하고, 항목 안에 미완료율을 함께 보여준다.

오늘 업무함에는 **전체 업무 목록**으로 들어가는 탭이나 버튼을 둔다. 전체 업무 목록은 마감일 없는 업무, 미완료 업무, 완료 업무를 모두 조회하는 화면이다. 보관 상태(`ARCHIVED`) 항목은 기본으로 숨기고, 보관 필터를 켰을 때만 보여준다. 기본 정렬은 `미완료 먼저`, `마감일 빠른 순`, `최근 수정 순`이다.

### 공문 상세 화면

공문 상세 화면은 공문 하나를 처리하기 위한 체크리스트이다.

- 기본 정보: 제목, 접수일, 마감일, 업무 유형, 제출 대상
- 처리 정보: 상태, 담당자 메모, 내 처리 메모
- 위치 정보: 외부 링크, 문서번호, 메신저 위치, 로컬 폴더 위치
- 연결 정보: 관련 제출물 수합판, 관련 템플릿

담당자 메모는 개인용 앱의 맥락에서 `부장교사`, `행정실`, `학년부`, `내가 처리`처럼 업무 출처나 협조 대상을 적는 용도이다. 권한을 가진 담당자를 배정하는 기능은 첫 버전에 넣지 않는다.

### 제출물 수합판

제출물 수합판은 표 중심 화면이다. 행은 학생, 열은 제출물 또는 제출 상태이다. 첫 버전에서는 수합판 하나가 제출물 하나를 관리한다.

각 행에는 학생 번호, 이름, 제출 상태, 제출일, 메모가 있다. 교사는 상태를 빠르게 바꿀 수 있고, 미제출 학생만 필터링할 수 있다.

학생 명부 입력은 두 가지를 지원한다.

- 직접 입력: 번호와 이름을 한 명씩 추가
- CSV/표 붙여넣기: `번호,이름` 형식의 목록을 붙여넣어 일괄 추가

### 템플릿함

템플릿함은 반복 문구를 모아두는 개인 자료실이다. 템플릿은 제목, 유형, 본문, 태그, 마지막 사용일을 가진다.

템플릿 유형은 다음과 같이 시작한다.

- 안내문
- 상담 기록 양식
- 보고 문구
- 제출 독촉 문구
- 기타

템플릿은 공문 상세 화면이나 제출물 수합판에서 복사해 사용할 수 있다. 첫 버전에서는 문구를 클립보드에 복사하거나 화면에서 미리보기만 제공한다. 외부 시스템으로 자동 발송하지 않는다.

### 마감 캘린더

마감 캘린더는 월간 보기와 주간 보기를 제공한다. 항목 색상은 유형으로 정한다.

- 학교 업무: 공문과 학교 전체 업무
- 학급 업무: 제출물, 학급 안내, 학급 운영 업무
- 개인 업무: 교사가 직접 만든 개인 마감

캘린더 항목을 선택하면 관련 공문, 제출물 수합판, 개인 마감 상세로 이동한다.

## 모듈 구조

앱은 여섯 개의 주요 모듈로 나눈다.

### 1. 개인 작업공간 모듈

첫 버전은 교사 1명의 개인 작업공간을 전제로 한다. 모든 데이터는 `userId`에 속한다. 사용자는 여러 학급을 만들 수 있지만, 데이터 공유 기능은 없다.

사용자는 로그인한 교사 한 명이며, 첫 버전에서는 역할도 `OWNER` 하나만 둔다. 다른 사용자 초대, 공동 편집, 권한 이전은 제공하지 않는다.

### 2. 학급·학생 명부 모듈

학급은 `classId`, 학년도, 학교급, 학년, 반 이름을 가진다. 학생은 `studentId`, `classId`, `studentNumber`, 이름, 표시명을 가진다.

`studentId`는 시스템 내부 식별자이고, `studentNumber`는 학급 안에서 교사가 관리하는 번호이다. CSV/표 붙여넣기에서는 번호와 이름을 읽고, 같은 학급 안에서 중복 번호가 있으면 가져오기를 중단하고 수정 안내를 보여준다.

### 3. 업무 항목 모듈

공문과 개인 마감은 공통 업무 항목으로 관리한다. 업무 항목은 제목, 유형, 캘린더 분류, 마감일, 상태, 메모, 위치 링크 목록을 가진다. 공문 유형은 추가로 `submissionTarget` 필드를 가진다. 이 값은 `교육지원청`, `교무부`, `학년부`, `행정실`, `학부모`, `학생`, `기타`처럼 제출하거나 전달해야 할 대상을 적는 텍스트 필드이다.

업무 유형은 다음 enum으로 관리한다.

| 표시 유형 | 내부 유형 | 생성 경로 |
| --- | --- | --- |
| 공문 | `OFFICIAL_DOCUMENT` | 공문 처리 체크리스트에서 생성 |
| 제출물 | `CLASS_SUBMISSION` | 제출물 수합판 생성 시 자동 생성 또는 공문에서 연결 생성 |
| 개인 마감 | `PERSONAL_DUE` | 오늘 업무함이나 캘린더에서 직접 생성 |

캘린더 분류는 색상 표시를 위한 값이다.

| 표시 분류 | 내부 분류 | 기본 연결 유형 |
| --- | --- | --- |
| 학교 업무 | `SCHOOL` | `OFFICIAL_DOCUMENT` |
| 학급 업무 | `CLASS` | `CLASS_SUBMISSION` |
| 개인 업무 | `PERSONAL` | `PERSONAL_DUE` |

교사는 필요하면 공문을 학급 업무로, 제출물을 학교 업무로 바꾸는 정도의 수동 분류 변경을 할 수 있다. 단, 업무 유형 자체는 생성 경로에 따라 유지한다.

업무 상태는 하나의 enum으로 관리한다.

| 표시 상태 | 내부 상태 | 의미 |
| --- | --- | --- |
| 접수 | `RECEIVED` | 해야 할 일이 들어왔지만 아직 처리하지 않음 |
| 처리 중 | `IN_PROGRESS` | 작업 중 |
| 제출 대기 | `WAITING_SUBMISSION` | 자료는 준비됐고 제출만 남음 |
| 완료 | `DONE` | 처리 완료 |
| 보관 | `ARCHIVED` | 완료 후 목록에서 숨김 |

### 4. 위치 링크 모듈

첨부 파일 대신 위치 정보를 저장한다. 위치 링크는 제목, 유형, 값, 메모를 가진다.

위치 유형은 다음과 같다.

- URL
- 업무포털 문서번호
- 학교 메신저 위치
- 로컬 폴더 위치
- 기타 메모

URL은 기본 형식을 검증한다. URL이 아닌 위치 정보는 일반 텍스트로 저장한다.

### 5. 제출물 수합 모듈

수합판은 제목, 연결 학급, 마감일, 연결 업무 항목, 상태 목록을 가진다. 각 학생별 수합 행은 `collectionId + studentId`로 식별한다.

공문과 수합판의 연결은 첫 버전에서 **공문 1개 : 수합판 여러 개** 관계를 허용한다. 하나의 수합판은 최대 하나의 공문에만 연결된다. 공문 없이 독립 수합판을 만들 수도 있으며, 이 경우에도 `CLASS_SUBMISSION` 업무 항목과 캘린더 항목은 생성된다.

수합 상태는 다음 enum으로 관리한다.

| 표시 상태 | 내부 상태 | 의미 |
| --- | --- | --- |
| 미제출 | `MISSING` | 아직 받지 않음 |
| 제출 | `SUBMITTED` | 제출 완료 |
| 보완 필요 | `NEEDS_REVISION` | 받았지만 수정이나 추가 자료가 필요함 |
| 해당 없음 | `NOT_APPLICABLE` | 이 학생에게 해당하지 않음 |

수합판의 완료율은 `해당 없음`을 제외한 학생 중 `제출` 상태인 비율로 계산한다.

### 6. 템플릿 모듈

템플릿은 제목, 유형, 본문, 태그, 치환값 목록, 마지막 사용일을 가진다. 템플릿은 실제 학생 상담 기록이나 민감한 사건 기록을 저장하는 곳이 아니라 반복 양식과 표현을 재사용하기 위한 도구이다.

지원하는 기본 치환값은 `{학급}`, `{마감일}`, `{제출물명}`, `{준비물}`, `{담임명}`이다. 값이 비어 있으면 치환하지 않고 원문 그대로 남긴다.

## 데이터 흐름

기본 흐름은 다음과 같다.

1. 교사가 학급을 만들고 학생 명부를 직접 입력하거나 CSV/표로 붙여넣는다.
2. 교사가 공문 업무를 추가하고 마감일, 제출 대상, 위치 링크를 입력한다.
3. 공문에 학생 제출물이 필요하면 제출물 수합판을 연결한다.
4. 제출물 수합판은 연결 학급의 학생 명부로 학생별 체크 행을 만든다.
5. 교사가 제출 상태를 체크하면 수합판 완료율과 미제출 목록이 갱신된다.
6. 공문, 제출물 수합판, 개인 마감은 마감 캘린더에 표시된다.
7. 오늘 업무함은 마감일, 상태, 제출 완료율을 바탕으로 오늘 볼 항목을 모아 보여준다.
8. 템플릿은 공문 메모나 제출 독촉 문구 작성 시 복사해 사용한다.

오늘 업무함의 시간 기준은 고정한다.

- `오늘 마감`: 지역 날짜 기준 오늘 마감인 미완료 항목
- `지난 마감`: 마감일이 지났지만 `DONE` 또는 `ARCHIVED`가 아닌 항목
- `이번 주 예정`: 오늘부터 7일 안에 마감되는 미완료 항목
- `미완료 제출물`: 마감일이 오늘 이전이거나 7일 안이고, 제출 완료율이 100%가 아닌 수합판

오늘 업무함은 항목별 대표 영역을 하나만 정한다.

1. 마감일이 지난 미완료 항목은 `지난 마감`에 표시한다.
2. 마감일이 오늘인 미완료 항목은 `오늘 마감`에 표시한다.
3. 제출 완료율이 100%가 아니고 마감일이 7일 안인 수합판은 위 두 조건에 해당하지 않을 때 `미완료 제출물`에 표시한다.
4. 마감일이 내일부터 7일 안인 미완료 항목은 위 조건에 해당하지 않을 때 `이번 주 예정`에 표시한다.
5. 마감일이 없거나 `DONE`, `ARCHIVED` 상태인 항목은 오늘 업무함의 긴급 영역에 표시하지 않는다.

## 민감 정보 처리

첫 버전은 학생 제출 여부와 일반 행정 메모만 다룬다. 실제 상담 내용, 생활지도 사건, 보호자 연락 기록은 저장 대상으로 삼지 않는다.

템플릿함의 `상담 기록 양식`은 빈 양식과 반복 문구만 저장한다. 특정 학생의 상담 내용이나 민감한 기록을 누적 관리하는 기능은 제공하지 않는다.

파일 업로드를 제공하지 않으므로 앱은 공문 원본, 동의서 스캔본, 학생 사진을 직접 저장하지 않는다. 위치 링크와 메모에 민감 정보가 들어갈 수 있으므로, 화면에는 개인용 데이터라는 안내를 제공한다.

## 오류와 예외 처리

첫 버전에서 반드시 다룰 오류 상황은 다음과 같다.

- 학생 명부가 비어 있으면 제출물 수합판 생성 전에 명부 추가를 안내한다.
- CSV/표 붙여넣기에서 번호나 이름이 비어 있으면 해당 행을 표시하고 가져오기를 막는다.
- 같은 학급 안에서 학생 번호가 중복되면 가져오기를 막고 중복 번호를 보여준다.
- URL 형식이 잘못되면 URL 위치로 저장하지 않고, 일반 메모 위치로 저장할지 선택하게 한다.
- 마감일이 없는 업무는 오늘 업무함의 긴급 영역과 캘린더에는 표시하지 않고, 전체 업무 목록에서만 확인하게 한다.
- 공문과 연결된 수합판을 삭제하려 하면 수합판과 학생별 체크 기록이 사라지고, 공문에서는 연결만 제거된다는 경고를 보여준다.
- 공문을 삭제하려 하면 연결된 수합판은 자동 삭제하지 않고 독립 수합판으로 남길지 함께 삭제할지 선택하게 한다.
- 학급을 삭제하려 하면 학생 명부, 연결된 수합판, 학생별 체크 기록, 수합판이 만든 `CLASS_SUBMISSION` 업무 항목과 캘린더 항목이 함께 사라진다는 경고를 보여준다. 해당 수합판이 공문에 연결되어 있으면 공문의 연결 정보도 함께 제거한다.
- 템플릿 치환값이 비어 있으면 치환하지 않고 원문 토큰을 유지한다.

삭제와 연결 해제의 정합성 규칙은 다음과 같다.

| 동작 | 함께 처리할 데이터 |
| --- | --- |
| 수합판 삭제 | 학생별 체크 행 삭제, 연결된 `CLASS_SUBMISSION` 업무 항목 삭제, 캘린더 항목 제거, 공문 연결 제거 |
| 공문 삭제 후 수합판 유지 | 공문 업무 항목 삭제, 수합판의 공문 연결 제거, 수합판의 `CLASS_SUBMISSION` 업무 항목 유지 |
| 공문 삭제 후 수합판 함께 삭제 | 공문 업무 항목 삭제, 연결 수합판과 학생별 체크 행 삭제, 수합판의 `CLASS_SUBMISSION` 업무 항목 삭제 |
| 학급 삭제 | 학생 명부 삭제, 학급에 속한 수합판과 체크 행 삭제, 관련 `CLASS_SUBMISSION` 업무 항목과 캘린더 항목 삭제, 공문 연결 제거 |

## 검증 기준

구현 후 다음 흐름이 실제로 동작해야 한다.

1. **공문 업무 흐름**
   - 공문 업무를 만든다.
   - 마감일, 제출 대상, 담당자 메모, 위치 링크를 입력한다.
   - 상태를 `접수`에서 `처리 중`, `완료`로 바꾼다.
   - 오늘 업무함과 캘린더에서 상태 변화가 반영된다.

2. **제출물 수합 흐름**
   - 학급과 학생 명부를 직접 입력한다.
   - CSV/표 붙여넣기로 명부를 가져온다.
   - 제출물 수합판을 만든다.
   - 학생별 상태를 `미제출`, `제출`, `보완 필요`, `해당 없음`으로 바꾼다.
   - 미제출 필터와 완료율이 맞게 계산된다.

3. **공문과 수합판 연결 흐름**
   - 공문에서 제출물 수합판을 연결한다.
   - 수합판 마감이 오늘 업무함과 캘린더에 나타난다.
   - 수합이 완료되지 않은 경우 오늘 업무함에 미완료 제출물로 표시된다.

4. **템플릿 재사용 흐름**
   - 템플릿을 만든다.
   - 치환값이 있는 문구를 미리보기한다.
   - 공문 메모나 제출물 안내 문구로 복사해 사용할 수 있다.

5. **파일 저장 제외 흐름**
   - 첨부 위치를 URL, 문서번호, 메신저 위치, 로컬 폴더 위치로 저장한다.
   - 실제 파일 업로드 입력이 제공되지 않는다.

## 성공 기준

- 담임교사가 오늘 업무함만 보고 오늘 처리할 행정 업무를 파악할 수 있다.
- 공문 업무의 마감, 위치, 상태, 연결 제출물을 한 화면에서 확인할 수 있다.
- 제출물 수합판에서 학생별 제출 여부와 미제출 학생을 빠르게 확인할 수 있다.
- 자주 쓰는 문구를 템플릿으로 저장하고 실제 업무 메모에 재사용할 수 있다.
- 마감 캘린더가 공문, 제출물, 개인 업무를 색상별로 구분해 보여준다.
- 앱이 파일 원본과 민감한 상담·생활지도 기록을 직접 저장하지 않는다.
